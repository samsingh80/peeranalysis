_schema-version: "3.2"
ID: peeranalysis
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: peeranalysis-destination-content
  type: com.sap.application.content
  requires:
  - name: peeranalysis-destination-service
    parameters:
      content-target: true
  - name: peeranalysis-repo-host
    parameters:
      service-key:
        name: peeranalysis-repo-host-key
  - name: peeranalysis-uaa
    parameters:
      service-key:
        name: peeranalysis-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: peeranalysis_html_repo_host
          ServiceInstanceName: peeranalysis-html5-service
          ServiceKeyName: peeranalysis-repo-host-key
          sap.cloud.service: peeranalysis
        - Authentication: OAuth2UserTokenExchange
          Name: peeranalysis_uaa
          ServiceInstanceName: peeranalysis-xsuaa-service
          ServiceKeyName: peeranalysis-uaa-key
          sap.cloud.service: peeranalysis
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: peeranalysis-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: peeranalysis-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - peeranalysis.zip
      name: peeranalysis
      target-path: resources/
- name: peeranalysis
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: peeranalysis-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: peeranalysis-destination-service
    service-plan: lite
- name: peeranalysis-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: peeranalysis-xsuaa-service
    service-plan: application
- name: peeranalysis-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: peeranalysis-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
